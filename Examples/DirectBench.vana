// Direct inline benchmark using StdBenchmark
function Main() {
    print("Starting DirectBench with StdBenchmark...");
    
    // Warmup / Start
    StdBenchmark.Start();
    
    // Loop 100M times
    double iterations = 100000000.0;
    
    for (int i = 0; i < 100000000; i = i + 1) {
       // Empty loop
    }
    
    StdBenchmark.End(iterations);
    
    print("Done.");
}
