// Direct inline benchmark - no function calls
function Main() {
    number i = 0;
    number sum = 0;
    
    StdTime.Measure();
    
    // 10 Million iterations
    while (i < 10000000) {
        sum = sum + i;
        i = i + 1;
    }
    
    number elapsed = StdTime.Measure();
print(elapsed);
if (elapsed == 0) {
    print("Elapsed is zero!");
    elapsed = 1; // Prevent division by zero
}
number ops = 10000000 * 1000000000 / elapsed;
print(ops);
}
