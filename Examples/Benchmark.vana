function Measure() {
    // Native time usage per Master Plan (StdTime.Now)
    double start = StdTime.Now(); 
    for (int i = 0; i < 1000000; i = i + 1) {
    }
    double end = StdTime.Now();
    print("Time: " + (end - start));
    // print("Ops: " + 10000000); // String + int works now
}

function Main() {
    Measure();
}
