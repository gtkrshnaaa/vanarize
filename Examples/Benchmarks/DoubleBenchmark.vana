// =========================================================================
// DoubleBenchmark.vana - Double (64-bit FP) Performance Benchmark
// =========================================================================
// Tests: 64-bit floating-point arithmetic operations
// Target: 1,000,000,000 operations/second
// =========================================================================

function Main() {
    print("=== DOUBLE (64-bit FP) BENCHMARK ===");
    
    int iterations = 100000000;
    double accumulator = 0.0;
    
    StdBenchmark.Start();
    
    for (int i = 0; i < iterations; i = i + 1) {
        accumulator = accumulator + 1.0;
    }
    
    StdBenchmark.End(iterations);
    
    print(accumulator);
}
